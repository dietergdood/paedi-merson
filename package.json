{
  "name": "paedi-merson-transfer",
  "version": "1.0.0",
  "description": "Pädi Merson — Der Transfer JGA App",
  "scripts": {
    "build": "node build.js",
    "dev": "npx serve ."
  },
  "engines": {
    "node": ">=18"
  }
}
